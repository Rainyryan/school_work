#include <iostream>
#include <map>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    map<int ,int> Mem;
    
    int N, M;
    cin>>N>>M;

    while(N--){
        string M; 
        int a, b;

        cin>>M;
        M[0] == 'g'? cin>>a: cin>>a>>b;
        


    }
}